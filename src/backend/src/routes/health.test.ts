import { describe, it, expect } from 'vitest';

describe('health route', () => {
  it('placeholder — replace with full integration test once test DB is wired', () => {
    expect(true).toBe(true);
  });
});
