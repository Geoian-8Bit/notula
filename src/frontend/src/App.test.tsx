import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('placeholder — replace with full RTL render test once test setup is wired', () => {
    expect(true).toBe(true);
  });
});
